kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: secure-platform
nodes:
  - role: control-plane
  - role: workekind: Cluster
    apiVersion: kind.x-k8s.io/v1alpha4
      name: secure-platform
        nodes:
            - role: control-plane
                - role: worker
